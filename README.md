# longterm-stock-scorer

- Python (original): `python scorecard.py [SYMBOLS...]`
- PHP equivalent entrypoint: `php scorecard.php [SYMBOLS...]`

The PHP script delegates to `scorecard.py` so the Python scoring logic remains unchanged.
